---
description: Commit the implementation with a proper commit message
argument-hint:
---

# Commit Implementation

The implementation is complete and validated.

## Commit Guidelines

Use conventional commits format:
- `feat:` for new features
- `fix:` for bug fixes
- `refactor:` for refactoring
- `docs:` for documentation
- `test:` for adding tests
- `chore:` for maintenance

## Your Mission

1. Check what files have been changed:
   ```bash
   git status
   ```

2. Stage all relevant files:
   ```bash
   git add <files>
   ```

3. Create a commit with a clear message:
   ```bash
   git commit -m "feat: <description of what was implemented>"
   ```

Do NOT push yet - just create the local commit.
